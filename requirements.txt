flask
ddgs
numpy
scipy
soundfile
psutil
pytest
pydantic>=2.10,<3

# Kaggle GPU training backend (hybrid/training/kaggle_training.py) -- lightweight,
# no torch, safe in the normal torch-free runtime environment. Only the
# `kaggle` CLI/executable is actually invoked (via subprocess); this repo
# never imports the `kaggle` Python package itself. See docs/history/kaggle_training.md.
kaggle>=2.2.4
